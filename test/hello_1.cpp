#include <iostream>
using namespace std;

// shift + ctrl + p 添加task（就是使用g++编译的过程）
// shift + alt + f  格式化代码
// shift + ctrl + d 仅编译
/**
* ctrl + p 快速导航文件
* ctrl + shift + tab 循环浏览最后一组打开的文件
* 
* ctrl + shift + o 导航到文件中的特定符号
* ctrl + g  导航到文件中的特定航
*/
int main(int argc, char const *argv[])
{
  cout << "hello, world" << endl;
  int a = 0;
  // std::system("pause");
  return 0;
}